# 新增工具使用说明

## 1. 独立CDN和IP反查工具包 📦

### 文件位置
```
standalone_cdn_ip_toolkit.py
```

### 功能特点
- ✅ **完全独立**：无依赖项目特定配置，可直接复制到其他项目使用
- ✅ **内置CDN数据**：包含主流CDN IP段和域名关键词
- ✅ **多种检查模式**：基础IP段检查 + 高级反向解析检查  
- ✅ **批量处理**：支持文件批量过滤
- ✅ **详细输出**：JSON格式输出，便于程序集成

### 使用方法

#### 1️⃣ 检查单个IP是否为CDN
```bash
# 基本检查
python3 standalone_cdn_ip_toolkit.py --check-ip 1.1.1.1

# 详细模式
python3 standalone_cdn_ip_toolkit.py --check-ip 1.1.1.1 --verbose

# JSON输出
python3 standalone_cdn_ip_toolkit.py --check-ip 1.1.1.1 --json
```

#### 2️⃣ IP反向解析
```bash
# 获取IP对应的域名
python3 standalone_cdn_ip_toolkit.py --reverse-ip 8.8.8.8

# JSON格式输出
python3 standalone_cdn_ip_toolkit.py --reverse-ip 8.8.8.8 --json
```

#### 3️⃣ 域名正向解析
```bash
# 获取域名对应的IP
python3 standalone_cdn_ip_toolkit.py --forward-domain google.com

# JSON格式输出  
python3 standalone_cdn_ip_toolkit.py --forward-domain google.com --json
```

#### 4️⃣ 批量过滤文件中的CDN IP
```bash
# 过滤文件中的CDN IP
python3 standalone_cdn_ip_toolkit.py --filter-file ips.txt --output clean_ips.txt

# 详细模式，显示过滤过程
python3 standalone_cdn_ip_toolkit.py --filter-file ips.txt --output clean_ips.txt --verbose
```

#### 5️⃣ 使用额外CDN文件
```bash
# 使用自定义CDN IP段文件
python3 standalone_cdn_ip_toolkit.py --check-ip 1.1.1.1 --cdn-file custom_cdn.txt

# 使用多个CDN文件
python3 standalone_cdn_ip_toolkit.py --check-ip 1.1.1.1 --cdn-file cdn1.txt --cdn-file cdn2.txt
```

### 编程集成示例
```python
from standalone_cdn_ip_toolkit import CDNIPChecker

# 初始化检查器
checker = CDNIPChecker()

# 检查IP
is_cdn, reason, details = checker.check_ip("1.1.1.1")
print(f"IP: 1.1.1.1, CDN: {is_cdn}, 原因: {reason}")

# 反向解析
domains = checker.reverse_lookup("8.8.8.8")
print(f"8.8.8.8 反向解析: {domains}")

# 正向解析
ips = checker.forward_lookup("google.com")
print(f"google.com 正向解析: {ips}")

# 批量过滤
ip_list = ["1.1.1.1", "8.8.8.8", "192.168.1.1"]
clean_ips, cdn_ips = checker.filter_ips_from_list(ip_list)
print(f"清洁IP: {clean_ips}")
print(f"CDN IP: {[info['ip'] for info in cdn_ips]}")
```

---

## 2. Web专用调试日志系统 🐛

### 文件位置
```
web/web_debug_logger.py
```

### 功能特点
- ✅ **专门排查Web问题**：扫描状态、终端日志、进度条等
- ✅ **多层日志记录**：API请求、扫描状态、调试信息分类记录
- ✅ **自动问题分析**：检测常见问题并给出解决建议
- ✅ **实时监控**：支持实时查看扫描进度和状态变化
- ✅ **Web API集成**：提供HTTP接口便于在Web界面中查看

### 使用方法

#### 1️⃣ 命令行工具
```bash
# 生成完整调试报告
python3 web/web_debug_logger.py --report

# 分析特定域名的扫描问题
python3 web/web_debug_logger.py --domain ic-china.top --analyze

# 查看域名相关日志
python3 web/web_debug_logger.py --domain ic-china.top --logs 30
```

#### 2️⃣ Web API接口

**获取调试状态:**
```bash
curl -u admin:password http://localhost:5000/api/debug/status
```

**分析特定域名:**
```bash
curl -u admin:password http://localhost:5000/api/debug/domain/ic-china.top
```

#### 3️⃣ 自动记录的信息

**API请求日志** (`logs/api_requests.log`)：
- 请求方法、端点、用户、响应时间
- 响应状态码、数据大小
- 请求参数和响应摘要

**扫描状态日志** (`logs/scan_status.log`)：
- 扫描启动、进度更新、完成事件
- 状态文件变化、阶段切换
- 错误和异常情况

**Web调试日志** (`logs/web_debug.log`)：
- 详细的调试信息和错误堆栈
- 数据加载过程和结果
- 性能指标和异常情况

#### 4️⃣ 常见问题自动检测

系统会自动检测并报告：
- ❌ 扫描状态文件缺失或格式错误
- ❌ 字段名不匹配问题（stages vs scan_stages）
- ❌ 扫描进度卡住（超过5分钟无更新）
- ❌ 扫描进程异常退出
- ❌ 数据解析错误

### 日志分析示例

**查看API响应时间:**
```bash
grep "duration" logs/api_requests.log | tail -10
```

**查看扫描状态变化:**
```bash
grep "scan_status" logs/scan_status.log | tail -10
```

**查看错误信息:**
```bash
grep "ERROR" logs/web_debug.log | tail -10
```

---

## 3. 问题排查流程 🔧

### Web界面数据显示问题
1. 访问: `http://localhost:5000/api/debug/domain/你的域名`
2. 查看 `analysis.issues` 字段中的问题诊断
3. 按照 `analysis.recommendations` 中的建议操作

### 扫描进度卡住问题
1. 检查扫描进程: `ps aux | grep scan.sh`
2. 查看状态文件: `cat output/域名/scanning_status.json`
3. 检查日志: `python3 web/web_debug_logger.py --domain 域名 --analyze`

### API响应慢问题
1. 查看API日志: `tail logs/api_requests.log`
2. 找到耗时超过1000ms的请求
3. 检查对应的调试日志了解原因

---

## 4. 集成状态 ✅

- ✅ **CDN工具**：已完全独立，可直接使用
- ✅ **Web日志**：已集成到Web应用，自动记录
- ✅ **API端点**：已添加调试接口
- ✅ **问题检测**：已启用自动分析功能

### 验证集成
启动Web服务后查看日志：
```bash
tail -f logs/web_debug.log
```

应该看到类似输出：
```
2024-01-01 12:00:00 [INFO] Web调试日志系统初始化完成
2024-01-01 12:00:01 [INFO] 获取域名详细数据 | 数据: {"domain": "test.com", "user": "admin"}
```

---

🎉 **工具已就绪，可以开始使用！**
